#include <iostream>                     //#include is a preprocessor directive that causes compiler to replace that line with the text 
using namespace std;                    //of contents of the named source file(if included in quotes "") or named header(if included in
                                        //angle brackets<>)
int main()                              //Execution of every C++ program begins with the main() function                      
{
cout<<"Hello World"<<endl;
return 0;
}
